import sys
import os
#accepts the validated deletion file as input, prints only the deletions larger than 500 bp
inputFile=sys.argv[1];

for line in open(inputFile):
    content=line.split("\t")
    if(int(content[3])-int(content[2]) > 500):
        print(line.strip())


